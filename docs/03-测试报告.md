# 测试报告

> 高校实验室设备借用管理系统  
> 版本：v2.0 | 日期：2026-06-19（修订版，与代码一致）

---

## 1. 测试概述

| 项目 | 说明 |
|------|------|
| 测试范围 | 用户认证、设备管理、借用管理、通知提醒、数据统计 |
| 测试类型 | 功能测试、接口测试 |
| 测试工具 | test_api.sh（Shell 脚本）、test_integration.py（Python） |
| 测试环境 | Windows 10 + Python 3.11 + Flask 3.x + SQLite |

## 2. 测试用例

### 2.1 用户认证

| 编号 | 测试项 | 输入 | 预期结果 | 实际结果 | 状态 |
|------|--------|------|----------|----------|------|
| TC-01 | 管理员登录 | admin / admin123 | 返回 token + 用户信息（role=admin） | token 正常返回 | ✅ |
| TC-02 | 错误密码登录 | admin / wrong | 返回 401 错误提示 | 返回 "用户名或密码错误" | ✅ |
| TC-03 | 新用户注册 | 新用户名+密码 | 返回 "注册成功" 201 | 注册成功 | ✅ |
| TC-04 | 重复用户名注册 | 已存在用户名 | 返回 409 错误提示 | 返回 "用户名已存在" | ✅ |
| TC-05 | 修改密码 | 旧密码+新密码 | 返回 "密码修改成功" | 修改成功 | ✅ |

### 2.2 设备管理

| 编号 | 测试项 | 输入 | 预期结果 | 实际结果 | 状态 |
|------|--------|------|----------|----------|------|
| TC-06 | 获取设备列表 | GET /api/equipment | 返回 JSON 数组 | 正常返回 | ✅ |
| TC-07 | 按名称搜索设备 | ?q=示波器 | 模糊匹配结果 | 正常返回 | ✅ |
| TC-08 | 按状态筛选设备 | ?status=available | 仅返回可借设备 | 正常返回 | ✅ |
| TC-09 | 管理员添加设备 | POST 设备信息 | 返回 "添加成功" + id | 添加成功 | ✅ |
| TC-10 | 管理员编辑设备 | PUT 新信息 | 返回 "更新成功"（自动计算库存） | 更新成功 | ✅ |
| TC-11 | 管理员删除设备 | DELETE | 有未归还记录时拒绝 | 拒绝删除 | ✅ |
| TC-12 | 未登录操作设备 | 无 Authorization | 返回 401 | 401 返回 | ✅ |
| TC-13 | 学生操作设备 | student 角色 Token | 返回 403 | 403 返回 | ✅ |

### 2.3 借用管理

| 编号 | 测试项 | 输入 | 预期结果 | 实际结果 | 状态 |
|------|--------|------|----------|----------|------|
| TC-14 | 提交借用申请 | 学生 + 设备ID + 预计归还时间 | 返回 "借用成功"，库存-1 | 借用成功 | ✅ |
| TC-15 | 借用已无库存设备 | 可用量=0的设备 | 返回 "该设备已无可用库存" | 正确拒绝 | ✅ |
| TC-16 | 有逾期记录时借用 | 有逾期未还的学生 | 返回 "请先归还后再借用" | 正确拒绝 | ✅ |
| TC-17 | 查询个人借用记录 | GET /api/borrow/records | 返回学生本人记录 | 正常返回 | ✅ |
| TC-18 | 管理员查看全量记录 | 管理员 Token | 返回所有用户记录 | 正常返回 | ✅ |
| TC-19 | 归还设备 | POST return | 返回 "归还成功"，库存+1 | 归还成功 | ✅ |
| TC-20 | 重复归还 | 对已归还记录再次归还 | 返回 "该设备已归还" | 正确拒绝 | ✅ |

### 2.4 通知提醒

| 编号 | 测试项 | 输入 | 预期结果 | 实际结果 | 状态 |
|------|--------|------|----------|----------|------|
| TC-21 | 查看通知列表 | GET /api/notifications | 返回用户通知数组 | 正常返回 | ✅ |
| TC-22 | 获取未读数 | GET unread-count | 返回 count 字段 | 正常返回 | ✅ |
| TC-23 | 标记已读 | POST /read | 返回 "已标记为已读" | 标记成功 | ✅ |
| TC-24 | 逾期自动生成通知 | 借用到期后访问任意API | notifications 表新增逾期通知 | ⬜ 待测 | ⬜ |
| TC-25 | 管理员手动提醒（指定记录） | POST /send {borrow_id} | 返回 "提醒已发送" | ⬜ 待测 | ⬜ |
| TC-26 | 管理员手动提醒（指定用户） | POST /send {user_id} | 返回 "提醒已发送" | ⬜ 待测 | ⬜ |
| TC-27 | 管理员批量提醒 | POST /send {} | 向所有逾期用户发送 | ⬜ 待测 | ⬜ |

### 2.5 数据统计

| 编号 | 测试项 | 输入 | 预期结果 | 实际结果 | 状态 |
|------|--------|------|----------|----------|------|
| TC-28 | 设备借用统计 | GET statistics/equipment | 返回含借用次数的设备列表 | 正常返回 | ✅ |
| TC-29 | 用户借用统计 | GET statistics/users | 返回含借用/逾期次数的用户列表 | 正常返回 | ✅ |
| TC-30 | CSV导出 | GET statistics/export | 返回带 BOM 的 CSV 文件 | 正常导出 | ✅ |

## 3. 测试执行

### 执行命令

```bash
# 方式一：启动系统
cd C:\Users\14396\lab-equipment-system
python app.py

# 方式二：Python 集成测试（自动测试核心流程）
python test_integration.py

# 方式三：Shell 脚本快速验证
bash test_api.sh
```

### 测试结果汇总

| 模块 | 用例数 | 已测通过 | 待测 | 通过率 |
|------|--------|---------|------|--------|
| 用户认证 | 5 | 5 | 0 | 100% |
| 设备管理 | 8 | 8 | 0 | 100% |
| 借用管理 | 7 | 7 | 0 | 100% |
| 通知提醒 | 7 | 3 | 4 | 43% |
| 数据统计 | 3 | 3 | 0 | 100% |
| **合计** | **30** | **26** | **4** | **87%** |

## 4. 缺陷记录

| 编号 | 严重程度 | 描述 | 状态 |
|------|----------|------|------|
| — | — | 无已知缺陷 | — |

## 5. 测试结论

核心业务流程（注册→登录→查看设备→借用→归还→统计导出）全部通过。通知模块基础功能（列表/未读数/已读标记）已验证通过，逾期自动通知和手动提醒功能（TC-24~27）为 v2.0 新增功能，待回归测试。
